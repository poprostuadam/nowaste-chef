[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "nowaste-chef"
version = "0.1.0"
description = "Aplikacja NoWaste Chef"
# Tu NIE MOŻE być linijki 'packages'

[tool.setuptools]
packages = ["main"]